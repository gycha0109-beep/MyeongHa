name: DB Commerce Payment Attempt Handoff Context

on:
  pull_request:
    paths:
      - 'supabase/migrations/**'
      - 'test/db/commerce_payment_attempt_handoff_context.sh'
      - '.github/workflows/db-commerce-payment-attempt-handoff-context.yml'
  push:
    branches:
      - main
    paths:
      - 'supabase/migrations/**'
      - 'test/db/commerce_payment_attempt_handoff_context.sh'
      - '.github/workflows/db-commerce-payment-attempt-handoff-context.yml'

permissions:
  contents: read

jobs:
  payment-attempt-handoff-context:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_HOST_AUTH_METHOD: trust
          POSTGRES_DB: myeongha_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres -d myeongha_test"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=10
    env:
      PGHOST: localhost
      PGPORT: 5432
      PGUSER: postgres
      PGDATABASE: myeongha_test
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install PostgreSQL client
        run: sudo apt-get update && sudo apt-get install -y postgresql-client
      - name: Bootstrap Supabase auth test fixture
        run: psql -v ON_ERROR_STOP=1 -f test/db/bootstrap_supabase_auth_stub.sql
      - name: Apply clean migrations
        shell: bash
        run: |
          set -euo pipefail
          for migration in supabase/migrations/*.sql; do
            echo "Applying ${migration}"
            psql -v ON_ERROR_STOP=1 -f "$migration" >/dev/null
          done
      - name: Run Commerce Payment Attempt handoff context tests
        run: bash test/db/commerce_payment_attempt_handoff_context.sh
      - name: Verify deterministic schema catalog snapshot
        run: bash test/db/catalog_snapshot.sh
