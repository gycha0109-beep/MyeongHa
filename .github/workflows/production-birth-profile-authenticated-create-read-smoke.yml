name: Production Birth Profile Authenticated Create Read Smoke

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type VERIFY_BIRTH_AUTHENTICATED_CREATE_READ to perform the one-time persistent production Birth smoke write.'
        required: true
        type: string

permissions:
  actions: read
  contents: read

concurrency:
  group: production-birth-profile-authenticated-create-read-smoke
  cancel-in-progress: false

jobs:
  verify:
    name: verify-production-birth-profile-authenticated-create-read
    runs-on: ubuntu-24.04
    environment: production
    env:
      DISPATCH_CONFIRM: ${{ inputs.confirm }}
      GH_TOKEN: ${{ github.token }}
      MYEONGHA_PRODUCTION_BIRTH_SMOKE_MEMBER_BEARER: ${{ secrets.MYEONGHA_PRODUCTION_BIRTH_SMOKE_MEMBER_BEARER }}
      MYEONGHA_PRODUCTION_BIRTH_SMOKE_MEMBER_EXPECTED_SUBJECT_ID: ${{ secrets.MYEONGHA_PRODUCTION_BIRTH_SMOKE_MEMBER_EXPECTED_SUBJECT_ID }}
    steps:
      - name: Verify explicit dispatch and dedicated write-smoke credential
        shell: bash
        run: |
          set -euo pipefail
          [[ "$DISPATCH_CONFIRM" == 'VERIFY_BIRTH_AUTHENTICATED_CREATE_READ' ]]

          prior_successes="$(
            gh api               "repos/${GITHUB_REPOSITORY}/actions/workflows/production-birth-profile-authenticated-create-read-smoke.yml/runs?event=workflow_dispatch&status=success&per_page=1"               --jq '.total_count'
          )"
          [[ "$prior_successes" == '0' ]]

          test -n "${MYEONGHA_PRODUCTION_BIRTH_SMOKE_MEMBER_BEARER:-}"
          test -n "${MYEONGHA_PRODUCTION_BIRTH_SMOKE_MEMBER_EXPECTED_SUBJECT_ID:-}"

      - name: Check out exact repository verifier
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node 24
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: '24'
          package-manager-cache: false

      - name: Verify authenticated production Birth create and owner read-after-create
        run: node scripts/verify-production-birth-profile-authenticated-create-read.mjs
