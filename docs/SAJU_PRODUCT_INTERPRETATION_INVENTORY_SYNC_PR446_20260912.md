# 명하 Saju Product Interpretation Inventory — PR #446 Authority Sync

> Repository: `gycha0109-beep/MyeongHa`  
> Purpose: delta sync after `SAJU_PRODUCT_INTERPRETATION_INVENTORY_SYNC_PR444_20260912.md`  
> Status: **GENERAL NATAL TRANSPARENCY SLOT PARTIAL EVIDENCE / RESEARCH-ONLY / P0-CM-03 OPEN / NO-BUILD**  
> MyeongHa base main: `7df690ed12e9292230cb766e9ff6b6f1d4a2ce1e`  
> Saju merged main: `8c5d949e31cd0a2c0f3ccad39920eb62f21a2f1b`  
> Authority delta input: Saju PR `#446` — `research(general-natal): bind partial transparency slot source evidence`

---

## 1. Verdict

Saju PR `#446` narrows the mixed-qi `透干` frontier by binding **positive source-example usage** to three canonical heavenly-stem positions:

```text
year
month
hour
```

This is deliberately **not** an exhaustive admissible-slot set.

The newly governed statement is only:

```text
selected mixed-qi source scope applies
AND a month hidden stem has an exact visible match at year/month/hour
-> positive source-scoped transparency existence evidence may be recorded
```

The following remain unresolved:

```text
day-slot admissibility
slot-set exhaustiveness
full transparency predicate
transparency selection effect
candidate construction
establishment judgment
```

Therefore:

```text
GENERAL_NATAL_PRODUCTION_AUTHORITY = BLOCKED
P0-CM-03 = OPEN
General Natal saleable = NO
Commerce = HOLD
Decision = NO_BUILD
```

---

## 2. Source evidence newly bound

PR `#446` binds two placement examples from the selected `子平真詮評注 / 論雜氣如何取用` context.

### Year / hour positive placement evidence

```text
甲生辰月，壬戊財印兩透，如財印分居年時
```

Bounded authority:

```text
透 is explicitly discussed together with year/hour placement
-> year and hour are positive source-observed transparency positions
```

This does not prove that year/hour are exclusive or exhaustive.

### Month positive placement evidence

```text
如甲生辰月，而為丙年壬辰月
...
蓋壬透自辰
```

Bounded authority:

```text
壬 is placed at the month stem
AND the commentary explicitly describes 壬 as transparent from 辰
-> month is a positive source-observed transparency position
```

This does not prove that month is mandatory or exclusive.

---

## 3. Canonical bridge gained

PR `#441` already records, for every month hidden stem:

```text
visibleExactStemPositions
exactVisibilityObserved
```

PR `#446` now permits the following narrow projection inside the PR `#444` mixed-qi scope `{辰,戌,丑,未}`:

```text
exact match at year/month/hour
-> positive_source_scoped_transparency_observed
```

The report-level authority is:

```text
sourceObservedTransparencySlots                    = {year, month, hour}
sourceObservedSlotSetExhaustive                    = false
daySlotAdmissibilityAuthorized                     = false
positiveTransparencyExistenceOnObservedSlotsAuthorized = true
fullTransparencyPredicateAuthorized                = false
transparencySelectionPredicateAuthorized           = false
branchMeetingSelectionEffectAuthorized             = false
multipleCandidateRepresentationAuthorized          = false
candidateDerivationAuthorized                      = false
establishmentPredicateAuthorized                   = false
candidateFactsEmitted                              = false
establishmentFactsEmitted                          = false
```

---

## 4. Day stem remains explicitly unresolved

A day-only exact match is not interpreted as either valid or invalid `透干`.

```text
day-only exact match
-> day_slot_match_source_admissibility_unresolved
```

MyeongHa must not infer:

```text
day is excluded
```

and must not infer:

```text
day is included
```

from the current source frontier.

Likewise this set:

```text
{year, month, hour}
```

must never be rewritten as:

```text
ALL_ALLOWED_TRANSPARENCY_SLOTS
```

It is only a non-exhaustive positive-example set.

---

## 5. Plural visibility remains evidence, not precedence

If the same month hidden stem appears at more than one source-observed position, PR `#446` preserves the plurality.

Example shape:

```text
visibleExactStemPositions = [year, hour]
-> positive source-scoped transparency existence = established
-> both observed positions retained
```

It does not establish:

```text
winner position
priority position
stronger transparency
single canonical candidate
candidate precedence
```

The earlier source statement `兼透則兼用` remains relevant evidence that plurality must not be silently collapsed, but a production candidate multiplicity contract is still missing.

---

## 6. Five coarse predicate-authority gaps remain OPEN

No coarse blocker is closed by Saju PR `#446`.

```text
MONTH_ORDER_HIDDEN_STEM_SELECTION_PREDICATE_AUTHORITY_MISSING
VISIBLE_STEM_TRANSPARENCY_SELECTION_PREDICATE_AUTHORITY_MISSING
BRANCH_MEETING_SELECTION_EFFECT_AUTHORITY_MISSING
MULTIPLE_GEJU_CANDIDATE_REPRESENTATION_AUTHORITY_MISSING
GEJU_ESTABLISHMENT_SUCCESS_FAILURE_PREDICATE_AUTHORITY_MISSING
```

The transparency gap remains open because the following are still missing:

```text
day-slot inclusion/exclusion authority
exhaustive slot-set authority
清 / conflict / precedence semantics
full source-to-canonical transparency selection predicate
```

Thus:

```text
positive transparency existence evidence
!= complete transparency-selection authority
```

---

## 7. Inventory delta

| Inventory item | State after Saju PR #444 | State after Saju PR #446 | Production impact |
|---|---|---|---|
| mixed-qi source scope `{辰,戌,丑,未}` | executable | unchanged | none |
| canonical exact visible-stem observations | executable | unchanged | none |
| year stem as positive transparency source example | unbound | **observed / bound** | none |
| month stem as positive transparency source example | unbound | **observed / bound** | none |
| hour stem as positive transparency source example | unbound | **observed / bound** | none |
| `{year,month,hour}` exhaustiveness | unauthorized | **still unauthorized** | blocked |
| day-stem admissibility | unresolved | **unresolved** | blocked |
| positive transparency existence on observed slots | unavailable | **authorized research evidence** | none |
| full transparency-selection predicate | unauthorized | **unauthorized** | blocked |
| branch-meeting selection/effect predicate | missing | **missing** | blocked |
| zero/one/multiple candidate representation | missing | **missing** | blocked |
| `GEJU_CANDIDATE` producer | unauthorized | **unauthorized** | blocked |
| establishment success/failure predicate | unauthorized | **unauthorized** | blocked |
| General Natal production authority | blocked | **blocked** | blocked |
| Commerce | HOLD | **HOLD** | no build |

---

## 8. Saju verification evidence

Saju PR `#446` final exact head:

```text
2d131d6fdd345258afc6bf20df0e84b863ab38b8
```

Exact-head gates:

```text
CI #2634                               = SUCCESS
Production Calculation Container #689 = SUCCESS
PIE Prospective Shadow #988            = SUCCESS
```

The exact-head CI included the new transparency-slot source-evidence tests and completed the repository-wide suite successfully.

Squash-merged Saju main:

```text
8c5d949e31cd0a2c0f3ccad39920eb62f21a2f1b
```

Merged-main gates:

```text
CI #2636                               = SUCCESS
Production Calculation Container #691 = SUCCESS
```

A concurrent Relationship research merge advanced Saju main immediately before PR `#446` landed. The actual parent-to-merge comparison was revalidated and still contained exactly the same three General Natal research files from PR `#446`; no unrelated PR `#446` semantic diff was introduced.

These gates verify repository integrity and the bounded partial-evidence contract only. They do not grant production interpretation or Commerce authority.

---

## 9. Product / API / Character / Commerce boundary

Forbidden substitutions include:

```text
{year, month, hour} observed positively
-> exhaustive slot set

day not yet evidenced
-> day excluded

visible exact match at observed slot
-> automatic GEJU_CANDIDATE

plural positive transparency evidence
-> application-defined winner / precedence

source-scoped evidence
-> establishment true/false
```

The following layers cannot fill these missing semantics:

```text
ProductHost
Character
LLM
API presentation
browser UX
SKU / price
PSP / payment
entitlement
refund
Commerce
```

---

## 10. Next authority frontier

The next source-backed frontier can now separate two questions cleanly.

### A. Transparency completion

If stronger authority is found, establish:

```text
day-slot inclusion/exclusion
slot-set exhaustiveness
full transparency predicate
```

Until then the transparency gap remains open.

### B. Branch meeting / 會支 evidence

The same selected mixed-qi source explicitly defines branch meeting as another selection axis, for example:

```text
甲生辰月，逢申與子會局，則用水印
```

Existing Saju three-combination research already distinguishes:

```text
full three-branch structural bureau formation
!= post-interaction effective bureau
!= transformation established
```

Therefore the next honest General Natal branch-meeting frontier should bind only source-scoped `會支` evidence to an already governed full-three structural bureau substrate. It must not treat a structural match as an automatically effective/transformed bureau, and must not emit `GEJU_CANDIDATE` or establishment state until the remaining semantic bridges are separately authorized.

---

## 11. Final boundary

```text
GENERAL_NATAL_PRODUCTION_AUTHORITY = BLOCKED
P0-CM-03 = OPEN

sourceObservedSlotSetExhaustive         = false
daySlotAdmissibilityAuthorized          = false
fullTransparencyPredicateAuthorized     = false
transparencySelectionPredicateAuthorized = false
branchMeetingSelectionEffectAuthorized  = false
multipleCandidateRepresentationAuthorized = false
candidateDerivationAuthorized           = false
establishmentPredicateAuthorized        = false
candidateFactsEmitted                   = false
establishmentFactsEmitted               = false

General Natal saleable = NO
Commerce = HOLD
Decision = NO_BUILD
```
