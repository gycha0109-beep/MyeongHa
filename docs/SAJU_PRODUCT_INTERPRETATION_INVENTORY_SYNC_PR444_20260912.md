# 명하 Saju Product Interpretation Inventory — PR #444 Authority Sync

> Repository: `gycha0109-beep/MyeongHa`  
> Purpose: delta sync after `SAJU_PRODUCT_INTERPRETATION_INVENTORY_SYNC_PR441_20260912.md`  
> Status: **GENERAL NATAL MIXED-QI SOURCE-SCOPE DELTA / RESEARCH-ONLY / P0-CM-03 OPEN / NO-BUILD**  
> MyeongHa base main: `c0fbc1ced9dfd054b28a9b2c54b4dda3efdae97d`  
> Saju merged main: `e5e0d6af4c37588932bdc7b1a017aa2bbbede44f`  
> Authority delta input: Saju PR `#444` — `research(general-natal): bind mixed-qi transparency source scope`

---

## 1. Verdict

Saju PR `#444` narrows the selected `子平真詮 / 子平真詮評注` mixed-qi source passage into one executable source-scope predicate.

Newly governed:

```text
MIXED_QI_MONTH_SOURCE_SCOPE_PREDICATE = AUTHORIZED

canonical month branch in { 辰, 戌, 丑, 未 }
  -> selected 四墓 / 雜氣 source passage applies
otherwise
  -> outside this selected source passage
```

The branch set is not an unstated traditional assumption. PR `#444` binds an explicit 徐樂吾 commentary cross-reference locator:

```text
section = 論雜氣如何取用
anchor  = 辰戌丑未四個月
```

This is a **source-scope authority gain only**. It does not authorize a canonical `透干` selection verdict, candidate producer, establishment judgment, General Natal product semantics, or Commerce.

```text
GENERAL_NATAL_PRODUCTION_AUTHORITY = BLOCKED

mixedQiMonthScopePredicateAuthorized         = true
canonicalPillarSlotAdmissibilityAuthorized   = false
exactMatchToTransparencySelectionAuthorized  = false
transparencySelectionPredicateAuthorized     = false
branchMeetingSelectionEffectAuthorized       = false
multipleCandidateRepresentationAuthorized    = false
candidateDerivationAuthorized                = false
establishmentPredicateAuthorized             = false
candidateFactsEmitted                        = false
establishmentFactsEmitted                    = false

General Natal saleable = NO
P0-CM-03               = OPEN
Production Payment     = HOLD
Decision               = NO_BUILD
```

---

## 2. Source semantics now bounded more precisely

Within the selected mixed-qi passage, the source frontier records the following bounded observations:

```text
四墓 / 雜氣 scope = 辰 戌 丑 未

一透則一用
兼透則兼用
透而又會，則透與會並用
```

Architecturally this means the selected source explicitly discusses:

```text
single transparency       -> single use observation
plural transparency       -> plural/co-use observation
transparency + branch meet -> joint-use observation
```

These are source-rule observations. They are **not yet canonical real-chart selection facts**.

The frontier does not generalize this selected passage to all twelve month branches.

---

## 3. Interaction with the PR #441 observation substrate

Saju PR `#441` already made the following canonical observations executable:

```text
derivedFacts.hiddenStems.month membership enumeration
visibleExactStemPositions for each month hidden stem
exactVisibilityObserved
```

PR `#444` deliberately does **not** join those raw observations directly to the classical source word `透`.

The selected source uses the wording `干 / 干頭`, but the governed passage does not enumerate a canonical slot policy such as:

```text
{ year, month, hour }
```

or:

```text
{ year, month, day, hour }
```

Therefore neither of those sets may be invented by the application.

```text
PR #441 exact visible match
    != authorized 透干 selection fact
```

until a governed source-to-canonical positional bridge exists.

---

## 4. New explicit provenance boundary

PR `#444` introduces an explicit branch-scope source reference:

```text
SRC-GEJU-ZIPING-ZHENQUAN-PINGZHU-FOUR-TOMB-MONTH-SCOPE
```

Bounded role:

```text
title   = 子平真詮評注
author  = 沈孝瞻
editor  = 徐樂吾
section = 論雜氣如何取用
anchor  = 辰戌丑未四個月
role    = branch-scope cross-reference only
```

It does **not** authorize:

```text
pillar-slot admissibility
hidden-stem ranking
exact match -> 透干 semantic promotion
branch-meeting transformation effect
candidate precedence
candidate representation
establishment success/failure
```

This separation is required so branch-scope provenance cannot silently acquire unrelated semantic authority.

---

## 5. Five coarse predicate-authority gaps remain OPEN

No coarse blocker is closed by Saju PR `#444`.

```text
MONTH_ORDER_HIDDEN_STEM_SELECTION_PREDICATE_AUTHORITY_MISSING
VISIBLE_STEM_TRANSPARENCY_SELECTION_PREDICATE_AUTHORITY_MISSING
BRANCH_MEETING_SELECTION_EFFECT_AUTHORITY_MISSING
MULTIPLE_GEJU_CANDIDATE_REPRESENTATION_AUTHORITY_MISSING
GEJU_ESTABLISHMENT_SUCCESS_FAILURE_PREDICATE_AUTHORITY_MISSING
```

The progress is narrower:

```text
before PR #444:
  selected source discussed 四墓 / 雜氣, but executable branch scope was not independently bound

after PR #444:
  selected-source month scope {辰,戌,丑,未} is explicit and executable
  transparency single/plural/joint-use source observations are explicit
  canonical transparency selection remains unresolved
```

Thus `VISIBLE_STEM_TRANSPARENCY_SELECTION_PREDICATE_AUTHORITY_MISSING` remains open.

---

## 6. Inventory delta

| Inventory item | State after Saju PR #441 | State after Saju PR #444 | Production impact |
|---|---|---|---|
| selected mixed-qi source month scope | source context only | **executable `{辰,戌,丑,未}` predicate** | none |
| explicit branch-scope provenance | implicit/bounded source context | **explicit 徐樂吾 locator bound** | none |
| single-transparency source semantics | source frontier evidence | **explicitly recorded in scoped boundary** | none |
| plural-transparency source semantics | source frontier evidence | **explicitly recorded in scoped boundary** | none |
| transparency + branch-meeting joint-use source semantics | source frontier evidence | **explicitly recorded in scoped boundary** | none |
| PR #441 exact visible-stem observation | executable observation | unchanged | none |
| canonical pillar-slot admissibility for 透干 | missing | **still missing** | blocked |
| exact visible match -> governed 透干 selection | unauthorized | **still unauthorized** | blocked |
| branch-meeting selection/effect predicate | missing | **still missing** | blocked |
| zero/one/multiple candidate representation | missing | **still missing** | blocked |
| `GEJU_CANDIDATE` producer | unauthorized | **still unauthorized** | blocked |
| establishment success/failure predicate | unauthorized | **still unauthorized** | blocked |
| General Natal production authority | blocked | **blocked** | blocked |
| Commerce | HOLD | **HOLD** | no build |

---

## 7. Saju verification evidence

Saju PR `#444` final exact head:

```text
2213b87e539d49c1ae5312d465885fb8624cd84c
```

Exact-head gates:

```text
CI #2629                               = SUCCESS
Production Calculation Container #684 = SUCCESS
PIE Prospective Shadow #983            = SUCCESS
PIE Prospective Shadow #984            = SUCCESS
```

Squash-merged Saju main:

```text
e5e0d6af4c37588932bdc7b1a017aa2bbbede44f
```

Merged-main gates:

```text
CI #2630                               = SUCCESS
Production Calculation Container #685 = SUCCESS
```

These gates verify repository integrity and the deterministic source-scope predicate only. They do not upgrade chart-selection, product, or commercial authority.

---

## 8. Product / API / Character / Commerce boundary

MyeongHa must not use the new mixed-qi source-scope predicate as a shortcut to manufacture a Gyeokguk result.

Forbidden substitutions remain:

```text
辰/戌/丑/未 month
  -> automatic GEJU_CANDIDATE

PR #441 visibleExactStemPositions
  -> automatic 透干

plural visible matches
  -> automatic plural candidate representation

source says 兼透則兼用
  -> application-defined precedence/conflict policy

source says 透而又會
  -> application-defined branch-meeting transformation effect
```

The following layers remain unable to fill the missing semantics:

```text
ProductHost
Character
LLM
API presentation
browser UX
SKU / price
PSP / payment
entitlement
refund
Commerce
```

---

## 9. Smallest honest next frontier

The next Saju research frontier is the **canonical transparency positional-admissibility bridge**.

It must answer with source-backed authority, not convention or implementation convenience:

```text
1. Which canonical heavenly-stem positions may satisfy the source word 透 / 干頭?
2. Is the day stem included, excluded, or context-dependent?
3. Is exact stem equality sufficient once positional scope is established?
4. How are zero, one, and plural admissible appearances represented before candidate construction?
5. Where does transparency selection end and branch-meeting effect begin?
```

Current reviewed evidence does not safely establish a universal canonical slot set. Examples elsewhere in the classical corpus name individual positions, while modern secondary explanations are not uniform enough to substitute for the missing governed bridge.

Therefore the system must remain fail-closed:

```text
canonicalPillarSlotAdmissibilityAuthorized  = false
transparencySelectionPredicateAuthorized    = false
candidateDerivationAuthorized               = false
establishmentPredicateAuthorized             = false
GENERAL_NATAL_PRODUCTION_AUTHORITY           = BLOCKED
P0-CM-03                                     = OPEN
Commerce                                     = HOLD
```
