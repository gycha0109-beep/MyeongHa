# FE040B — Canonical Registry Consumer Activation

Watchtower-Track: face-reading

FE040B activates the positive canonical-registry path in MyeongHa by consuming the immutable Saju distribution artifact. MyeongHa does not copy or redefine the canonical metric registry.

## Immutable upstream pin

- Saju distribution commit: `50fd5b511326033861b3cab48028b989c4499b3c`
- artifact path: `distribution/face-reading/fe040b/myeongha-face-reading-0.0.0.tgz`
- artifact SHA-256: `210cb8f4358fb3b111e0ad3ab7914ef6846ef14af80e781c31544a8fbc527706`
- canonical FE035B source commit: `0f7de13b18a9dd9966074f371cbfd9554490f0ef`
- canonical FE035B source blob: `c9ed7dfb347144759694056e89d571c433d4dfc8`

The package exposes the existing `./preview-engine` subpath and the governed `./product-neutral-observation-contract-fe035b` subpath.

## Positive registry admission

The FE040A bridge remains the admission implementation. FE040B changes the pinned artifact from the FE024 preview-only distribution to FE040B and requires the actual package subpath to load.

Admission still requires all FE040A checks:

- exact FE035B contract version;
- upstream assertion function;
- exact contract shape;
- four canonical regions;
- thirteen unique canonical metrics;
- eight required and five conditional metrics;
- closed per-metric semantic boundaries;
- closed authority boundary;
- closed evolution policy;
- deterministic SHA-256 registry fingerprint.

Artifact availability is not itself registry admission. Runtime validation remains mandatory.

## Positive static-observation admission

The positive test derives the observation surface from the issued registry object itself. It does not hard-code or locally redefine canonical metric identities.

All canonical metrics are supplied with finite synthetic neutral values and all regions are marked available. The existing FE040A static allowlist then validates metric identity, region, unit, presence, availability, and the issued-registry brand.

## Locked semantic boundary

FE040B does not issue:

- operationalization authority;
- traditional binding;
- threshold or calibration authority;
- classification, score, or ranking authority;
- structured claims;
- narrative authority;
- production interpretation authority;
- LLM semantic authority.

The FE038 interpretation shell remains locked with `criterion.state = not_admitted`, `claims = []`, and `narrative.allowed = false`.

The next semantic step remains FE041 OperationalizationDefinition review; FE040B does not cross that boundary.
