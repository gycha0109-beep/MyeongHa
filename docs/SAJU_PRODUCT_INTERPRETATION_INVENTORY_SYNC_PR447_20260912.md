# 명하 Saju Product Interpretation Inventory — PR #447 Authority Sync

> Repository: `gycha0109-beep/MyeongHa`  
> Purpose: delta sync after `SAJU_PRODUCT_INTERPRETATION_INVENTORY_SYNC_PR446_20260912.md`  
> Status: **GENERAL NATAL BRANCH-MEETING SOURCE EVIDENCE / RESEARCH-ONLY / P0-CM-03 OPEN / NO-BUILD**  
> MyeongHa base main: `83348b548d3a87beab5be8c3fff01589d1036e6e`  
> Saju merged main: `d5329e13d1ffbdf5e524bbea1d9a640942419c40`  
> Authority delta input: Saju PR `#447` — `research(general-natal): bind branch-meeting source evidence`

---

## 1. Verdict

Saju PR `#447` adds a deterministic research-layer bridge from selected `子平真詮 / 論雜氣如何取用` branch-meeting examples to canonical full-three structural relations.

The newly governed statement is only:

```text
selected mixed-qi month scope applies
AND canonical structuralRelations is resolved
AND an exact branch_three_combination includes the month pillar
AND its three branch values exactly match the selected source example
AND structuralMatchOnly = true
AND transformationEstablished = false
-> source-aligned 會支 evidence may be recorded
```

This does **not** mean:

```text
structural relation
-> transformation established
-> post-interaction effective bureau
-> final branch-meeting selection effect
-> GEJU_CANDIDATE
-> established Gyeokguk
```

Therefore the product boundary is unchanged:

```text
GENERAL_NATAL_PRODUCTION_AUTHORITY = BLOCKED
P0-CM-03 = OPEN
General Natal saleable = NO
Commerce = HOLD
Decision = NO_BUILD
```

---

## 2. Source-aligned meeting map now executable as evidence

The selected source examples are bound to the following exact full-three branch sets:

```text
辰 month -> 辰 + 申 + 子
丑 month -> 丑 + 巳 + 酉
未 month -> 未 + 亥 + 卯
戌 month -> 戌 + 寅 + 午
```

Canonical Korean branch-value mapping:

```text
진 -> {진, 신, 자}
축 -> {축, 사, 유}
미 -> {미, 해, 묘}
술 -> {술, 인, 오}
```

These are source-to-canonical evidence mappings, not production pattern verdicts.

### 辰 example

Selected source anchor:

```text
何謂會支？如甲生辰月，逢申與子會局，則用水印是也
```

Research authority gained:

```text
resolved 辰-month canonical full 辰申子 structural match
-> source-aligned 會支 evidence may be emitted
```

### 丑 example

Selected source anchor:

```text
又如甲生丑月，辛透為官，或巳酉會成金局
```

PR `#447` deliberately does not use this wording to invent a partial-combination production rule.

Required canonical substrate remains:

```text
full canonical 丑巳酉 branch_three_combination
```

### 未 example

Selected source anchor:

```text
如壬生未月，透己為官，而地支會亥卯以成傷官之局
```

Research authority gained:

```text
resolved 未-month canonical full 未亥卯 structural match
-> source-aligned 會支 evidence may be emitted
```

### 戌 example

Selected source anchor:

```text
如甲生戌月，透辛為官，而又透丁以傷官，月支又會寅會午以成傷官之局
```

Research authority gained:

```text
resolved 戌-month canonical full 戌寅午 structural match
-> source-aligned 會支 evidence may be emitted
```

---

## 3. Canonical bridge requirements

The bridge is fail-closed and requires all of the following:

```text
1. pillars.month.status = resolved
2. month branch is in selected mixed-qi scope {辰, 戌, 丑, 未}
3. derivedFacts.structuralRelations.status = resolved
4. relation.kind = branch_three_combination
5. relation contains exactly three branch participants
6. one participant is the month pillar with the resolved month branch
7. participant branch values cover the exact source-aligned set for that month
8. relation.semantics.structuralMatchOnly = true
9. relation.semantics.transformationEstablished = false
```

Only then may the research report record:

```text
sourceAlignedFullThreeMeetingObserved = true
sourceMeetingUseAxisEvidenceEstablished = true
```

The same evidence item simultaneously preserves:

```text
branchMeetingSelectionEffectEstablished = false
postInteractionEffectiveBureauEstablished = false
candidateEmitted = false
```

---

## 4. Structural match remains distinct from transformation and effectiveness

Canonical `branch_three_combination` semantics remain:

```text
structuralMatchOnly = true
transformationEstablished = false
```

MyeongHa must preserve the following separation:

```text
canonical structural match
!= transformation success
```

```text
source-aligned 會支 evidence
!= post-interaction effective bureau
```

```text
source-aligned 會支 evidence
!= fully settled branch-meeting selection effect
```

This is especially important because existing Saju three-combination research already recognizes that clash/competing interaction can break, damage, or otherwise alter a structurally formed bureau, while deterministic post-interaction settlement remains separate.

No MyeongHa product layer may skip that unresolved semantic stage.

---

## 5. Deterministic statuses gained

PR `#447` can now distinguish the following research states without inventing production authority:

```text
resolved_source_aligned_full_three_meeting_observed_effect_unresolved
resolved_no_source_aligned_full_three_meeting_observed
outside_selected_mixed_qi_scope
canonical_substrate_unavailable
```

This is a material source-grounding improvement because `會支` is no longer only a textual source boundary; it can now be bound to exact canonical chart evidence.

However, the positive state explicitly ends in:

```text
effect_unresolved
```

That suffix is semantically binding.

---

## 6. Authority gained vs authority not gained

### Newly executable research authority

```text
STRUCTURAL_MATCH_TO_SOURCE_MEETING_EVIDENCE_BRIDGE = AUTHORIZED
SOURCE_MEETING_USE_AXIS_EVIDENCE = AUTHORIZED
SOURCE_ALIGNED_BRANCH_MEETING_STRUCTURAL_EVIDENCE = AUTHORIZED
```

### Still unauthorized

```text
branch-meeting transformation predicate
post-interaction effective bureau predicate
clash/break/damage settlement for General Natal selection
清 / 濁 settlement
有情 / 無情 settlement
branch-meeting precedence against transparent stems
zero/one/multiple GEJU candidate representation
GEJU_CANDIDATE producer
GEJU_ESTABLISHMENT_STATE=true/false
General Natal production authority
ProductHost / Character / LLM substitution
API/browser production exposure
Commerce rollout
```

---

## 7. Five coarse predicate-authority gaps remain OPEN

No coarse blocker is closed by PR `#447`.

```text
MONTH_ORDER_HIDDEN_STEM_SELECTION_PREDICATE_AUTHORITY_MISSING
VISIBLE_STEM_TRANSPARENCY_SELECTION_PREDICATE_AUTHORITY_MISSING
BRANCH_MEETING_SELECTION_EFFECT_AUTHORITY_MISSING
MULTIPLE_GEJU_CANDIDATE_REPRESENTATION_AUTHORITY_MISSING
GEJU_ESTABLISHMENT_SUCCESS_FAILURE_PREDICATE_AUTHORITY_MISSING
```

The branch-meeting gap remains open because the new bridge proves only:

```text
source-aligned full-three meeting evidence exists
```

It does not yet prove:

```text
that the meeting remains effective after all relevant interactions
how clash/damage changes selection
how 透干 and 會支 coexist or compete in the final candidate representation
how 清/濁 or 有情/無情 settles conflicting source surfaces
```

Thus:

```text
SOURCE_ALIGNED_BRANCH_MEETING_STRUCTURAL_EVIDENCE = AUTHORIZED
```

while:

```text
BRANCH_MEETING_SELECTION_EFFECT_AUTHORITY_MISSING = OPEN
```

---

## 8. Inventory delta

| Inventory item | State after Saju PR #446 | State after Saju PR #447 | Production impact |
|---|---|---|---|
| mixed-qi month source scope `{辰,戌,丑,未}` | executable | unchanged | none |
| canonical exact visible-stem observation | executable | unchanged | none |
| positive transparency source-example evidence | executable research evidence | unchanged | none |
| canonical `branch_three_combination` structural substrate | available structural-only | unchanged | none |
| exact source-aligned 會支 branch sets | textual boundary | **executable research mapping** | none |
| structural-match -> source 會支 evidence bridge | unavailable | **authorized** | none |
| no-meeting / outside-scope / unavailable deterministic states | partial/unbound | **authorized** | none |
| transformation predicate | unauthorized | **unauthorized** | blocked |
| post-interaction effective bureau | unauthorized | **unauthorized** | blocked |
| branch-meeting final selection effect | missing | **still missing** | blocked |
| transparency-vs-meeting precedence | missing | **missing** | blocked |
| candidate multiplicity representation | missing | **missing** | blocked |
| `GEJU_CANDIDATE` producer | unauthorized | **unauthorized** | blocked |
| establishment success/failure predicate | unauthorized | **unauthorized** | blocked |
| General Natal production authority | blocked | **blocked** | blocked |
| Commerce | HOLD | **HOLD** | no build |

---

## 9. Saju exact-head verification evidence

Saju PR `#447` final exact head:

```text
1908c06fa461e1ff9257af3def57a683646f8dc7
```

Exact-head gates:

```text
CI #2637                               = SUCCESS
Production Calculation Container #692 = SUCCESS
PIE Prospective Shadow #989            = SUCCESS
```

PR merge preflight immediately before merge:

```text
fresh main = 8c5d949e31cd0a2c0f3ccad39920eb62f21a2f1b
head       = 1908c06fa461e1ff9257af3def57a683646f8dc7
mergeable  = true
ahead      = 3
behind     = 0
changed files = 3 research-only files
```

---

## 10. Saju merged-main verification evidence

Squash-merged Saju main:

```text
d5329e13d1ffbdf5e524bbea1d9a640942419c40
```

The actual merge parent remained:

```text
8c5d949e31cd0a2c0f3ccad39920eb62f21a2f1b
```

Parent-to-merge diff remained exactly the same three research files:

```text
docs/research/general-natal-geju-branch-meeting-source-evidence-20260912.md
src/research/general-natal-geju-branch-meeting-source-evidence.ts
test/general-natal-geju-branch-meeting-source-evidence.test.ts
```

Merged-main gates:

```text
CI #2639                               = SUCCESS
Production Calculation Container #694 = SUCCESS
```

No production runtime, ProductHost, Character, API/browser, or Commerce code was added by Saju PR `#447`.

---

## 11. MyeongHa integration authority after this sync

MyeongHa may treat the following as research-grade upstream facts:

```text
mixed-qi source scope is known
exact transparency observations exist
partial positive transparency slot evidence exists
source-aligned full-three 會支 structural evidence can be recognized
```

MyeongHa must still fail closed for:

```text
final 透干 selection
final 會支 selection effect
interaction settlement
candidate construction
candidate multiplicity/precedence
establishment success/failure
General Natal product exposure
Commerce
```

No LLM, Character prompt, ProductHost mapping, UI heuristic, or client-side rule may manufacture the missing authority.

---

## 12. Next semantic frontier

The smallest honest next frontier is not Commerce and not candidate emission.

It is the source-backed settlement layer between:

```text
source-aligned 會支 structural evidence
```

and:

```text
branch-meeting selection effect
```

The next research must determine, without importing unsupported rules:

```text
1. whether the selected source authorizes 會支 use directly from structural meeting or only under additional conditions;
2. how clash / break / damage affects the General Natal 會支 use path;
3. whether and how 透干 and 會支 coexist, reinforce, or conflict;
4. what source-backed semantics govern 清 / 濁 and 有情 / 無情 in those combined cases;
5. which evidence is merely descriptive and which can safely produce a candidate-stage selection signal.
```

Until that closes:

```text
BRANCH_MEETING_SELECTION_EFFECT_AUTHORITY_MISSING = OPEN
GEJU_CANDIDATE_DERIVATION_AUTHORITY_MISSING = OPEN
GEJU_ESTABLISHMENT_PREDICATE_AUTHORITY_MISSING = OPEN
GENERAL_NATAL_PRODUCTION_AUTHORITY = BLOCKED
P0-CM-03 = OPEN
Commerce = HOLD
```
