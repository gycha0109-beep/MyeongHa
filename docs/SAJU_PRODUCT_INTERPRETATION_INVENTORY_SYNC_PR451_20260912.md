# 명하 Saju Product Interpretation Inventory — PR #451 Authority Sync

> Repository: `gycha0109-beep/MyeongHa`  
> Purpose: delta sync after `SAJU_PRODUCT_INTERPRETATION_INVENTORY_SYNC_PR447_20260912.md`  
> Status: **GENERAL NATAL CO-USE / DIRECT AFFINITY EXEMPLAR EVIDENCE / RESEARCH-ONLY / P0-CM-03 OPEN / NO-BUILD**  
> MyeongHa base main: `98a005b498501891bdf2c5016de07a5c251d7435`  
> Saju merged main: `6ddde1e25970eeb87a5bfc9e680873af1dcf40c2`  
> Authority delta input: Saju PR `#451` — `research(general-natal): bind co-use affinity source evidence`

---

## 1. Verdict

Saju PR `#451` adds a research-only source bridge for the selected mixed-qi passage:

```text
一透則一用，兼透則兼用，透而又會，則透與會並用
```

and binds a narrow set of direct `有情 / 無情` exemplars without generalizing them into chart-wide success/failure rules.

The newly governed statements are only:

```text
positive source-scoped transparency evidence
+
source-aligned full-three 會支 evidence
-> transparency + branch-meeting co-use source evidence may be recorded
```

and, only for exact direct source exemplars:

```text
exact exemplar match
-> source-direct 有情 / 無情 evidence may be recorded
```

This does **not** mean:

```text
co-use evidence
-> canonical GEJU_CANDIDATE[]
```

or:

```text
direct 有情 / 無情 exemplar
-> generalized affinity predicate
-> generalized branch-meeting selection effect
-> 成格 / 敗格
```

Therefore the product boundary is unchanged:

```text
GENERAL_NATAL_PRODUCTION_AUTHORITY = BLOCKED
P0-CM-03 = OPEN
General Natal saleable = NO
Commerce = HOLD
Decision = NO_BUILD
```

---

## 2. Plural/coexisting source use is now executable as evidence

The selected source explicitly allows:

```text
一透則一用
兼透則兼用
透而又會，則透與會並用
```

Saju PR `#451` therefore authorizes the following research boundary:

```text
TRANSPARENCY_AND_MEETING_CO_USE_SOURCE_BOUNDARY = AUTHORIZED
SOURCE_PLURAL_CO_USE_BOUNDARY = OBSERVED
SINGLE_WINNER_REQUIREMENT = NOT_AUTHORIZED
```

MyeongHa must preserve the distinction:

```text
plural/coexisting source surfaces are allowed
!= canonical candidate multiplicity contract exists
```

No product layer may infer:

```text
one winner
priority order
numeric strength
candidate ranking
candidate deduplication
candidate identity
```

from the co-use source boundary.

---

## 3. Direct source exemplar affinity evidence

The source defines:

```text
有情 -> 順而相成
無情 -> 逆而相背
```

PR `#451` does not implement a generalized `有情 / 無情` predicate. It records only exact direct exemplars whose current canonical inputs can be matched without inventing extra semantic rules.

### 3.1 甲辰 / 癸 + 辰申子 — direct 有情 evidence

Required research match:

```text
day master = 甲
month branch = 辰
positive source-scoped transparency evidence contains 癸
source-aligned full-three meeting evidence contains 辰申子
```

Then:

```text
source_direct_having_affinity
```

may be recorded as source-direct exemplar evidence.

It must not be generalized into:

```text
all 印 + 水局 combinations are 有情
all such combinations establish 印綬格
```

### 3.2 壬未 / 己 + 未亥卯 — direct 無情 evidence

Required research match:

```text
day master = 壬
month branch = 未
positive source-scoped transparency evidence contains 己
source-aligned full-three meeting evidence contains 未亥卯
```

Then:

```text
source_direct_lacking_affinity
```

may be recorded.

It must not become a universal `官 / 傷官` conflict predicate or automatic failure verdict.

### 3.3 甲戌 / 辛丁 + 戌寅午 — direct 無情 evidence

Required research match:

```text
day master = 甲
month branch = 戌
positive source-scoped transparency evidence contains both 辛 and 丁
source-aligned full-three meeting evidence contains 戌寅午
```

Then:

```text
source_direct_lacking_affinity
```

may be recorded.

If either `辛` or `丁` is missing, the direct exemplar is not matched.

---

## 4. 丑 exemplar remains intentionally unencoded as affinity classification

The selected source also contains a `丑 / 辛 / 巳酉 / 己` example classified as `有情`.

PR `#451` intentionally does not convert that example into a canonical direct-affinity matcher because its source composition combines alternatives and multiple surfaces in a way that still requires a separate canonical interpretation decision.

Fail-closed boundary:

```text
ambiguous exemplar composition
-> no direct canonical affinity classification
```

The 丑 surface can still participate in the already authorized generic `透 + 會` co-use evidence where upstream PR #446/#447 inputs exist.

---

## 5. Upstream authority reused; no independent rule invention

PR `#451` reuses:

```text
PR #446 transparency source evidence
PR #447 branch-meeting source evidence
```

Transparency input is eligible only when upstream evidence already records:

```text
positiveTransparencyExistenceOnObservedSlotsEstablished = true
```

Meeting input is eligible only when upstream evidence already records an exact source-aligned full-three meeting.

Therefore #451 does not invent:

```text
new hidden-stem ranking
new transparency slots
new branch combination rules
new transformation rules
```

---

## 6. Deterministic research statuses gained

PR `#451` distinguishes:

```text
resolved_source_co_use_with_direct_exemplar_quality_evidence
resolved_source_co_use_observed_quality_unsettled
resolved_no_joint_transparency_meeting_evidence
outside_selected_mixed_qi_scope
canonical_substrate_unavailable
```

The `quality_unsettled` state is binding:

```text
co-use source evidence exists
BUT
this exact canonical combination is not one of the safely encoded direct 有情/無情 exemplars
-> no extrapolated quality verdict
```

---

## 7. Authority gained vs not gained

Research-only authority gained:

```text
TRANSPARENCY_AND_MEETING_CO_USE_SOURCE_BOUNDARY = AUTHORIZED
SOURCE_PLURAL_CO_USE_BOUNDARY = OBSERVED
DIRECT_SOURCE_EXEMPLAR_AFFINITY_EVIDENCE = AUTHORIZED
```

Still unauthorized:

```text
exhaustive transparency slot predicate
general transparency selection predicate
general branch-meeting selection-effect predicate
post-interaction effective bureau
general clash / break / damage settlement
general 清 / 濁 predicate
general 有情 / 無情 predicate
universal 官 / 傷官 conflict predicate
candidate ranking / precedence
zero/one/multiple canonical candidate contract
GEJU_CANDIDATE producer
GEJU_ESTABLISHMENT_STATE=true/false
General Natal production authority
Commerce
```

Direct source classification is evidence about a cited exemplar; it is not an establishment fact.

---

## 8. Five coarse predicate-authority gaps remain OPEN

No coarse blocker is closed by PR `#451`:

```text
MONTH_ORDER_HIDDEN_STEM_SELECTION_PREDICATE_AUTHORITY_MISSING
VISIBLE_STEM_TRANSPARENCY_SELECTION_PREDICATE_AUTHORITY_MISSING
BRANCH_MEETING_SELECTION_EFFECT_AUTHORITY_MISSING
MULTIPLE_GEJU_CANDIDATE_REPRESENTATION_AUTHORITY_MISSING
GEJU_ESTABLISHMENT_SUCCESS_FAILURE_PREDICATE_AUTHORITY_MISSING
```

The fourth gap is narrowed because the source now gives executable evidence that plural/coexisting use exists, but the gap remains open because there is still no authorized canonical contract for:

```text
candidate identity
candidate array representation
deduplication
precedence
ranking
```

The third gap remains open because co-use evidence and direct exemplar quality do not settle generalized post-interaction branch-meeting effectiveness.

---

## 9. Saju verification evidence

Saju PR `#451` final exact head:

```text
0bbc10fef3e62e5ab8e61bc5587f197244203236
```

Exact-head gates:

```text
CI #2645                               = SUCCESS
Production Calculation Container #700 = SUCCESS
PIE Prospective Shadow #995            = SUCCESS
```

Saju squash-merged main:

```text
6ddde1e25970eeb87a5bfc9e680873af1dcf40c2
```

Merged-main gates:

```text
CI #2646                               = SUCCESS
Production Calculation Container #701 = SUCCESS
```

The merged commit changed only the intended General Natal research surface inherited from PR #451.

---

## 10. Product / Commerce decision

MyeongHa must continue to treat Saju as the sole semantic authority.

No ProductHost, Character, LLM, API/browser, SKU, payment, entitlement, refund, or Commerce layer may manufacture the missing candidate or establishment semantics.

```text
Product semantic authority = NO
General Natal production authority = BLOCKED
Commerce implementation = HOLD / NO_BUILD
```

---

## 11. Next semantic frontier

The next honest research question is narrower than `GEJU_CANDIDATE` construction.

The source now supports:

```text
一透則一用
兼透則兼用
透而又會，則透與會並用
```

and PR `#451` makes the plural/co-use boundary executable as evidence.

Therefore the next frontier may investigate a research-only canonical representation for:

```text
zero / one / multiple selection signals
```

while preserving:

```text
selection signal != GEJU_CANDIDATE
selection signal != established Gyeokguk
```

A future signal contract must not silently invent:

```text
candidate identity
single-winner precedence
ranking
strength
establishment success/failure
```

If source/canonical evidence is insufficient to define a safe signal identity contract, the `MULTIPLE_GEJU_CANDIDATE_REPRESENTATION_AUTHORITY_MISSING` gap remains fully open rather than being guessed.

Until a separately governed candidate representation and establishment predicate exist:

```text
GENERAL_NATAL_PRODUCTION_AUTHORITY = BLOCKED
P0-CM-03 = OPEN
Commerce = HOLD
```
