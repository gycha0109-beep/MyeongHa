# 명하 Saju Product Interpretation Inventory — PR #473 Authority Sync

> Repository: `gycha0109-beep/MyeongHa`  
> Purpose: delta sync after `SAJU_PRODUCT_INTERPRETATION_INVENTORY_SYNC_PR455_20260912.md`  
> Status: **GENERAL NATAL CANDIDATE-IDENTITY ADMISSION REVIEW / RESEARCH-ONLY / SAJU_AUTHORITY_DEPENDENCY / P0-CM-03 OPEN / NO-BUILD**  
> MyeongHa base main: `47c19bad5721796d16fa43788d6e5efc0f16cd52`  
> Saju merged main: `80502139f16ca93255a8e5dd3f70e6a11ea66103`  
> Authority delta input: Saju PR `#473` — `research(general-natal): review candidate identity admission boundary`

---

## 1. Verdict

Saju PR `#473` reviews the narrow bridge left open after the governed selection-signal and semantic-use work:

```text
source selection / use observation
-> canonical pre-establishment candidate identity
-> establishment success / failure
```

The selected source directly contains source-use selection/coexistence, named pattern wording, formation-outcome wording, and exact primary/co-pattern wording. It does **not** define the separate intermediate canonical pre-establishment candidate stage required by the current architecture.

The binding admission decision is:

```text
SOURCE_USE_AND_PATTERN_WORDING_OBSERVED_PRE_ESTABLISHMENT_CANONICAL_CANDIDATE_STAGE_NOT_DEFINED
```

Therefore the productization boundary remains fail-closed:

```text
GENERAL_NATAL_PRODUCTION_AUTHORITY = BLOCKED
P0-CM-03 = OPEN
GEJU_CANDIDATE = NOT_EMITTED
GEJU_ESTABLISHMENT_STATE = NOT_EMITTED
Commerce = HOLD
Decision = NO_BUILD
```

This is an upstream `SAJU_AUTHORITY_DEPENDENCY`, not a MyeongHa Product Mapper, Character, LLM, runtime, SKU, or Commerce implementation gap that downstream code may fill.

---

## 2. Source observations frozen by Saju #473

The review preserves four source observations without promoting them into a generalized candidate model:

```text
SOURCE_USE_SELECTION_IDENTITY = OBSERVED
SOURCE_NAMED_PATTERN_WORDING = OBSERVED
SOURCE_FORMATION_OUTCOME_WORDING = OBSERVED
SOURCE_PRIMARY_CO_PATTERN_WORDING = OBSERVED
```

### Source-use selection and coexistence

The selected mixed-qi discussion states:

```text
一透則一用，兼透則兼用，透而又會，則透與會並用
```

This supports source-use selection and coexistence. It does not itself publish a repository-style `GEJU_CANDIDATE` object between use observation and formation judgment.

### Named pattern wording

The exact exemplar reviewed by Saju directly names a pattern (`印綬之格`) while also carrying quality/compatibility language. Direct pattern naming therefore exists, but it is not evidence of an independently defined pre-establishment candidate data model.

### Formation-outcome wording

The source also uses formation language such as:

```text
似成格矣
印格不成
```

This proves that formation semantics exist in the source. It does not authorize a generalized establishment predicate.

### Primary / co-pattern wording

The exact `論用神變化` example includes:

```text
格成正財，正官乃其兼格也
```

This establishes an exact source example in which a formed primary `格` and a `兼格` are named. Because the wording itself says `格成`, it cannot be generalized into a pre-formation candidate hierarchy without introducing a stage the source did not separately define.

---

## 3. Candidate bridge remains unauthorized

Saju #473 explicitly keeps the following authority flags false:

```text
sourceUseToCanonicalCandidateBridgeAuthorized = false
sourcePatternWordingToPreEstablishmentCandidateBridgeAuthorized = false
canonicalCandidateStageDefinitionAuthorized = false
semanticUseDeduplicationIntoCandidateAuthorized = false
primaryCoPatternGeneralizedPrecedenceAuthorized = false
candidateIdentityAuthorized = false
multipleCandidateRepresentationAuthorized = false
candidateDerivationAuthorized = false
```

MyeongHa must preserve these exact negative boundaries.

In particular:

```text
source semantic use != GEJU_CANDIDATE
source pattern wording != pre-establishment candidate identity
semantic-use deduplication != candidate deduplication
formed primary/co-pattern example != generalized candidate precedence
```

No downstream layer may infer candidate identity, count, rank, precedence, strength, deduplication, primary/secondary status, or winner selection from source-use IDs, source pattern names, array order, `格成`, or `兼格`.

---

## 4. Establishment remains a separate unresolved authority

Saju #473 preserves the existing architecture separation:

```text
selectionEstablishmentSeparationStillRequired = true
establishmentPredicateAuthorized = false
establishmentFactsEmitted = false
```

Therefore:

```text
GEJU_ESTABLISHMENT_STATE = NOT_EMITTED
```

Exact source phrases proving that formation success/failure is discussed do not by themselves define the complete deterministic inputs, interaction settlement, or generalized success/failure predicate needed by a canonical producer.

---

## 5. Multiple-candidate representation gap remains OPEN

The prior governed signal/semantic-use work allows plural source observations and directly observed semantic uses to coexist without destructive single-winner collapse.

Saju #473 additionally observes an exact formed primary `格` plus `兼格` example.

Neither result defines the missing pre-establishment candidate contract. The following remains OPEN:

```text
MULTIPLE_GEJU_CANDIDATE_REPRESENTATION_AUTHORITY_MISSING
```

Still unresolved are:

```text
what exact object is a candidate before establishment?
how is a source-use identity bridged to that object?
when are same-use observations one candidate or several candidate evidences?
how are source pattern words separated from already-formed pattern verdicts?
how are multiple candidate identities represented before any authorized precedence rule?
```

---

## 6. Five coarse Gyeokguk authority gaps remain OPEN

Saju #473 closes none of the five coarse gaps already tracked by MyeongHa:

```text
MONTH_ORDER_HIDDEN_STEM_SELECTION_PREDICATE_AUTHORITY_MISSING
VISIBLE_STEM_TRANSPARENCY_SELECTION_PREDICATE_AUTHORITY_MISSING
BRANCH_MEETING_SELECTION_EFFECT_AUTHORITY_MISSING
MULTIPLE_GEJU_CANDIDATE_REPRESENTATION_AUTHORITY_MISSING
GEJU_ESTABLISHMENT_SUCCESS_FAILURE_PREDICATE_AUTHORITY_MISSING
```

The candidate-identity review narrows the fourth gap by ruling out a terminology-only bridge, but it does not authorize the missing representation or producer.

---

## 7. Saju verification evidence

Final exact PR head:

```text
ce6387b59603eed4ec9dcea3b2d58275d4f50fcc
```

Exact-head gates:

```text
CI #2700                               = SUCCESS
Production Calculation Container #755 = SUCCESS
PIE Prospective Shadow #1033           = SUCCESS
```

Expected-head squash merge produced:

```text
80502139f16ca93255a8e5dd3f70e6a11ea66103
```

The merged change surface is exactly:

```text
docs/research/general-natal-geju-candidate-identity-admission-review-20260912.md
src/research/general-natal-geju-candidate-identity-admission-review.ts
test/general-natal-geju-candidate-identity-admission-review.test.ts
```

Saju issue `#472` is closed as a bounded research review. MyeongHa must not interpret that issue closure as satisfaction of General Natal production authority; the productization classification remains `SAJU_AUTHORITY_DEPENDENCY`.

---

## 8. MyeongHa downstream invariants

The following remain forbidden downstream substitutions:

```text
ProductHost-generated candidate identity
Character-generated candidate identity
LLM-generated candidate identity
API/browser fallback candidate identity
SKU-layer semantic stitching
payment/entitlement activation as semantic authorization
```

MyeongHa may expose the blocked capability state, but it may not manufacture the missing Saju-owned semantic authority.

The canonical Product Reading response contract being structurally admitted does not alter this rule:

```text
response contract admission
!= production interpretation semantic authority
```

---

## 9. Product / Commerce consequence

No product lifecycle gate advances from Saju #473.

```text
General Natal saleable = NO
P0-CM-03 = OPEN
Production Interpretation Authority = HOLD / BLOCKED
Public Production Reading runtime = BLOCKED
MyeongHa Production Product Reading transport = BLOCKED
GEJU_CANDIDATE = NOT_EMITTED
GEJU_ESTABLISHMENT_STATE = NOT_EMITTED
Production Payment = HOLD
Decision = NO_BUILD
```

No Product/Capability seed, Offer/Price activation, PSP call, entitlement grant, paid Reading finalization, `reading_refs`, or grounding may use this review as positive production authority.

---

## 10. Next honest frontier

Candidate identity cannot be promoted by aliasing source terminology into a new repository object.

The next upstream Saju work must target one of the actual missing semantic bridges:

1. source-backed generalized month-order hidden-stem selection predicate;
2. complete transparency selection predicate, including unresolved slot admissibility;
3. branch-meeting selection/effect semantics distinct from structural formation;
4. a source that explicitly distinguishes pre-establishment candidate identity from established `格` identity; or
5. generalized establishment success/failure semantics with all required interaction-settlement inputs.

Until a separately governed upstream change closes one of those dependencies:

```text
candidate identity authority remains BLOCKED
GENERAL_NATAL_PRODUCTION_AUTHORITY remains BLOCKED
Commerce remains HOLD
```
